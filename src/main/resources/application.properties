# Database Configuration
db.url=jdbc:mysql://localhost:3306/vet_clinic
db.username=root
db.password=root
db.poolSize=10

# Application Configuration
app.name=Vet Clinic
app.version=1.0.0

# Logging Configuration
logging.level=INFO
logging.file=logs/vet_clinic.log

# Email Configuration
mail.smtp.host=smtp.gmail.com
mail.smtp.port=587
mail.smtp.auth=true
mail.smtp.starttls.enable=true
