# Database Configuration
db.url=jdbc:mysql://localhost:3306/vet_clinic?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
db.username=root
db.password=
db.poolSize=10

# Email Configuration
mail.smtp.host=smtp.gmail.com
mail.smtp.port=587
mail.smtp.auth=true
mail.smtp.starttls.enable=true

# Logging Configuration
logging.level=INFO
logging.file=vet_clinic.log